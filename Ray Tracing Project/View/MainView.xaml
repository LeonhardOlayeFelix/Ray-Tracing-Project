<Window x:Class="Ray_Tracing_Project.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ray_Tracing_Project.View"
        xmlns:vm="clr-namespace:Ray_Tracing_Project.ViewModel"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="Ray Tracer App" Height="450" Width="800">
    <Window.Resources>
        <vm:ViewModelLocator x:Key="ViewModelLocator"/>
    </Window.Resources>
    <Window.DataContext>
        <Binding Source="{StaticResource ViewModelLocator}" Path="MainViewModel"/>
    </Window.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Width="456" Source="{Binding Source}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Width="90" Height="20" Content="Trace" Command="{Binding TraceCommand}" Margin="0 0 10 0"/>
        </StackPanel>
        <ProgressBar Grid.Row="2" Height="30" Width="500" Margin="0 20 0 20" Value="{Binding ProgressPercentage}"/>
    </Grid>
</Window>
